import * as crypto from 'crypto'

const createKeys = (password) => {
    //TODO: PrivateKeyEncryption(SHA256(password))
    //      Write keys to disk, PEM format
}

const encryptMessage = (publicKey, rawMessage) => {

}

const decryptMessage = (rawPrivateKey, encryptedMessage) => {

}

createKeys("supersecretpassword");
